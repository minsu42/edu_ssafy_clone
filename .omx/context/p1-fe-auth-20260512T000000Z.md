# Context Snapshot — P1-FE Auth Frontend

- task statement: Implement P1-FE from `docs/plan/edu-ssafy-com-wise-goose.md`.
- desired outcome: Add frontend auth infrastructure for login/logout/profile, router/layout baseline, store/session flow, component/unit tests, and Playwright scaffolding.
- known facts/evidence:
  - backend auth endpoints already exist under `/api/v1/auth/*` and `/api/v1/users/me`
  - current frontend only contains a smoke auth store and no router/pages
  - P1-FE plan requires router, layout, login page, profile page, store tests, component tests, and Playwright setup
- constraints:
  - keep changes scoped to frontend P1-FE
  - do not invent backend API contracts beyond `docs/api/auth.md`
  - ask the user only if plan/API/design are insufficient
- unknowns/open questions:
  - exact login success landing route naming versus later dashboard phase
  - whether dependency installation/networked verification is permitted in this session
- likely codebase touchpoints:
  - `frontend/package.json`
  - `frontend/src/main.ts`
  - `frontend/src/router/`
  - `frontend/src/layouts/`
  - `frontend/src/pages/auth/`
  - `frontend/src/stores/`
  - `frontend/src/test/handlers/`
  - `frontend/e2e/`
